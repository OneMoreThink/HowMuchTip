{
  "extends": "expo/tsconfig.base", // 이 부분은 그대로 유지합니다.
  "compilerOptions": {
    "strict": true,
    "paths": {
      // app 폴더에 대한 별칭 (Expo Router가 기본적으로 사용하는 경우가 많으므로 유지하는 것이 좋습니다)
      // 만약 app 폴더 내의 _layout.tsx 등에서 @/constants/Colors 등을 사용한다면 필요할 수 있습니다.
      // 필요 없다면 지워도 됩니다.
      "@/*": ["./*"],

      // src 폴더 내부의 경로들을 위한 별칭들
      // 예: import SomeComponent from '@/components/SomeComponent';
      "@/components/*": ["./src/components/*"],
      // 예: import { SomeHook } from '@/hooks/SomeHook';
      "@/hooks/*": ["./src/hooks/*"],
      // 예: import MyScreen from '@/screens/MyScreen';
      "@/screens/*": ["./src/screens/*"],
      // 예: import { SomeType } from '@/types';
      // types/index.ts 파일 자체를 참조합니다.
      "@/types": ["./src/types/index.ts"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ]
}